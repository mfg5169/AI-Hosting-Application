// import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   /* config options here */
// };

// export default nextConfig;

// next.config.mjs

// Import type definitions if necessary, but in JavaScript, you won't use types directly
// import type { NextConfig } from "next"; // This line can be omitted in .mjs

const nextConfig = {
    images: {
        domains: ['cdn-icons-png.flaticon.com']
    }
    // Your config options here
  };
  
  export default nextConfig;
  